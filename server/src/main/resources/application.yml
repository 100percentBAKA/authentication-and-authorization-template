# spring.application.name=security-demo
spring:
  application:
    name: security-demo

  datasource:
    url: jdbc:mysql://localhost:32768/testerDB
    username: root
    password: adarshgs
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    user:
      name: root
      password: root

